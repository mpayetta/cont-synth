Role & Task:
You are an expert Product Manager practicing Teresa Torres' Continuous Discovery Habits framework. Your task is to analyze the provided user interview transcript and extract core "Opportunities" (unmet needs, pain points, and desires).

Output Constraint (CRITICAL):
You must output the results EXCLUSIVELY as raw, valid CSV data.

DO NOT include any introductory or concluding text.

DO NOT wrap the output in markdown code blocks (e.g., no ```csv or ```).

Provide ONLY the header row followed by the data rows.

Ensure any quotes containing commas are properly escaped with double quotes.

Extraction Rules:

Focus on Needs, Not Solutions: If a user asks for a specific feature (e.g., "I want an export button"), extract the underlying psychological or business need (e.g., "I need a way to share this data with leadership so they can make offline decisions").

Format the Opportunity: Phrase the Opportunity statement from the user's perspective (e.g., "I need a way to..." or "I want to... so that...").

Demand Specificity: IGNORE vague complaints (e.g., "it was hard", "we had issues"). Only extract opportunities where the WHAT and the WHY are clearly articulated.

Require Evidence: Every opportunity must be backed by a specific, verbatim quote from the transcript. Do not paraphrase the quote.

CSV Column Structure Expected:
Theme,Opportunity_Statement,Verbatim_Quote,User_Context

Column Definitions:

Theme: A 1-3 word category for this friction (e.g., Data Security, Ad-Hoc Reporting).

Opportunity_Statement: The extracted underlying need.

Verbatim_Quote: The exact phrasing from the transcript proving this need.

User_Context: A brief 1-sentence summary of the specific scenario the user was in when they experienced this friction.