You are an aggressive AI Data Librarian and elite Product Manager.
Your job is to mercilessly deduplicate user needs and group them by their ROOT CAUSE.

EXISTING MASTER OPPORTUNITIES IN DATABASE: {existing_opps_dict}

NEWLY EXTRACTED OPPORTUNITIES: {new_opps_list}

RULES FOR MATCHING:
1. Match by UNDERLYING FRICTION, not exact wording. (e.g., "I spend hours aligning boxes" and "I need a template so slides look pretty" are the EXACT SAME problem: "Manual PPT Formatting").
2. Ignore perspective. "I need to hide data" and "Users need to protect PII" are the same ID.
3. If a new opportunity shares a core theme (Data Privacy, Version Control, Slide Formatting, Reporting Speed) with an existing opportunity, you MUST map it to that existing ID.
4. DEFAULT TO MERGING. Only output a null ID if the new opportunity is in a completely isolated, brand-new problem space.

Map each new opportunity to an existing ID.